# Image Classification Project

## Requirements

- Windows, macOS, or Linux
- Python 3.10 (the pinned TensorFlow version does not support every newer Python version)
- Git

## Setup on another laptop

Clone the repository and open a terminal in the project folder:

```bash
git clone https://github.com/Kanojkumar2001/2-1-sdc-soft-mod3-handson1.git
cd 2-1-sdc-soft-mod3-handson1
python -m venv .venv
```

Activate the virtual environment.

Windows PowerShell:

```powershell
.venv\Scripts\Activate.ps1
```

Windows Command Prompt:

```bat
.venv\Scripts\activate.bat
```

macOS/Linux:

```bash
source .venv/bin/activate
```

Install dependencies:

```bash
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

## Run the application

```bash
python -m streamlit run app.py
```

Then open the URL printed by Streamlit, normally `http://localhost:8501`.

The image quality analysis works without a trained CNN checkpoint. To enable CNN training and prediction, run:

```bash
python scripts/train_cnn.py
```

To train the hybrid model:

```bash
python scripts/train_hybrid.py
```

The command-line entry point is also available:

```bash
python main.py --mode predict --image data/test/00_001.png --model hybrid
```

All application, configuration, and model paths are resolved from the project directory, so the commands also work when launched from another current directory.

## Run with Docker

Install Docker Desktop on the laptop, then open a terminal in the cloned project folder. Docker Desktop works on Windows, macOS, and Linux and provides the same Python environment on each device.

Build the image:

```bash
docker build -t image-classifier .
```

Start the application:

```bash
docker run --rm -p 8501:8501 image-classifier
```

Open `http://localhost:8501` in a browser. Stop the application with `Ctrl+C`.

On ARM-based laptops, Docker may use amd64 emulation because the pinned TensorFlow version is packaged for amd64. Docker Desktop must have virtualization enabled.
